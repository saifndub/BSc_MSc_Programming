#include<stdio.h>
#include<math.h>
int me(int a, int b)
{
    int c = add(a + b);
    //return z;
}
int main()
{
    int x, y, z;
    scanf("%d %d", &x, &y);
    me(x, y);
    printf("sum is %d :", z);
}

